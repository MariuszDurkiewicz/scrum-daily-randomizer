# Scrum Daily Randomizer

## How to install
Use `yarn` command to install dependances.

## How to run
- To start api service run `yarn start:api`
- To start React application `yarn start:app`
- Go to http://localhost:8080 to start using
